import type { Metadata } from 'next'
import { Inter, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Vroha | Land Your Dream Job Faster',
  description: 'Career acceleration services - LinkedIn Profile Optimization, Resume Writing, Profile Marketing & Portfolio Creation. Get placed at top companies globally.',
  keywords: 'career services, LinkedIn optimization, resume writing, job placement, career acceleration, portfolio creation',
  generator: 'v0.app',
  openGraph: {
    title: 'Vroha | Land Your Dream Job Faster',
    description: 'Career acceleration services to help you land your dream job at top companies globally.',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
